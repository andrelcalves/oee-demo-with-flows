# Atlas AI agent external ID — enables live in-app chat (the FAB is always visible).
# Create or reuse a CDF Atlas agent whose system prompt is scoped to nitric-acid OEE
# monitoring (dashboard KPIs, areas, equipment, losses) and paste its externalId below.
# Leave empty for preview-only mode (FAB + demo panel, no API calls).
VITE_ATLAS_AGENT_EXTERNAL_ID=
